package algorithm;

import java.util.HashMap;
import java.util.Scanner;

public class BOJ_1181 {

	public static void main(String[] args) {
		String a = "hesitate", b = "yours";
		System.out.println(a.compareTo(b));
		int N;
		int  smallest;
		String string[], tmp;
		HashMap<String, Boolean> hashMap = new HashMap<String, Boolean>();
		
		Scanner scan = new Scanner(System.in);
		N = scan.nextInt();
		string = new String[N];
		
		for(int i= 0 ;i< N;i++) {
			tmp = scan.next();
			if(hashMap.get(tmp) == null) {
				hashMap.put(string[i], true);
				string[i] = tmp;
			}else {
				i--;
			}
		}
		
		//문자열 length() 비교 
		for(int i = 0 ;i < string.length-1;i++) {
			smallest = i;
			for(int j = i+1; j < string.length;j++) {
				if(string[j].length() < string[smallest].length()) {
					smallest = j;
				}
			}
			if(i != smallest) {//i가 자기 자신이 아닐 때 swap
				tmp = string[i];
				string[i] = string[smallest];
				string[smallest] = tmp;
			}
		}
		
		
		for(int i = 0 ;i < string.length-1;i++) {
			smallest = i;
			for(int j = i+1; j < string.length;j++) {
//				if(string[j].length() < string[smallest].length()) {
//					System.out.println("사이즈 비교 바꿀값은" + string[j]  + " 인덱스는  "+ i + " 최솟값은" + string[smallest]);
//					smallest = j;
//				}
				if(string[smallest].length() == string[j].length()) {
					if(string[smallest].compareTo(string[j]) > 0 ) {
						smallest = j;
					}
				}
			}
			if(i != smallest) {//i가 자기 자신이 아닐 때 swap
				System.out.println("원래 값은 " + string[i] + " 바꿀값은" + string[smallest] + " 인덱스는 " + i);
				tmp = string[i];
				string[i] = string[smallest];
				string[smallest] = tmp;
			}
		}

		System.out.println("");
		for(int i = 0;i < string.length; i++) {
			System.out.println(string[i]);
		}
	}
}
//선택 정렬
//inplace 알고리즘 중 하나
//
